# Python batch pipeline (backfill + training + inference).
# Loose floors so pip can pick wheels compatible with the installed Python.
psycopg[binary]>=3.2
pandas>=2.2
numpy>=2.1
scikit-learn>=1.5
httpx>=0.27
